// import section

// factory function to create node class
const Node = () => {
  // default null values
  const nodeValue = null;
  const nextNodeValue = null;
  // ability to return the info
  const value = () => nodeValue;
  const nextNode = () => nextNodeValue
}

// factory function to create linked lists
const LinkedList = () => {
  
  const append = (list, value) => {
    
  }
  
}

